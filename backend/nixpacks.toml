[phases.setup]
aptPkgs = ["wget", "xvfb", "libnss3", "libgconf-2-4", "libfontconfig1", "libasound2"]
cmds = [
  "wget -q https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb -O /tmp/chrome.deb",
  "apt-get install -y /tmp/chrome.deb",
  "rm /tmp/chrome.deb"
]
